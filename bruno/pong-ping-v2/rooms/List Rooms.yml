info:
  name: List Rooms
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/rooms"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
